<Window x:Class="StepWind.App.SwDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner" SizeToContent="Height" Width="440"
        Background="Transparent" AllowsTransparency="True"
        FontFamily="{StaticResource SW.Font.Text}">
  <Window.Triggers>
    <EventTrigger RoutedEvent="Window.Loaded">
      <BeginStoryboard>
        <Storyboard>
          <DoubleAnimation Storyboard.TargetName="DialogRoot" Storyboard.TargetProperty="Opacity"
                           From="0" To="1" Duration="0:0:0.16"/>
          <DoubleAnimation Storyboard.TargetName="DialogScale" Storyboard.TargetProperty="ScaleX"
                           From="0.94" To="1" Duration="0:0:0.20" EasingFunction="{StaticResource EaseOut}"/>
          <DoubleAnimation Storyboard.TargetName="DialogScale" Storyboard.TargetProperty="ScaleY"
                           From="0.94" To="1" Duration="0:0:0.20" EasingFunction="{StaticResource EaseOut}"/>
        </Storyboard>
      </BeginStoryboard>
    </EventTrigger>
  </Window.Triggers>
  <Border x:Name="DialogRoot" Background="{StaticResource SW.Card}" CornerRadius="12" BorderThickness="1"
          BorderBrush="{StaticResource SW.Stroke}" Padding="22,18" RenderTransformOrigin="0.5,0.5">
    <Border.RenderTransform><ScaleTransform x:Name="DialogScale" ScaleX="1" ScaleY="1"/></Border.RenderTransform>
    <StackPanel>
      <TextBlock x:Name="TitleText" FontFamily="{StaticResource SW.Font}" FontSize="16"
                 FontWeight="SemiBold" Foreground="{StaticResource SW.Text}"/>
      <TextBlock x:Name="MessageText" FontSize="12.5" Foreground="{StaticResource SW.Text2}"
                 TextWrapping="Wrap" Margin="0,10,0,0" LineHeight="19"/>
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
        <Button x:Name="CancelButton" Style="{StaticResource SW.Button.Ghost}" Content="Cancel"
                MinWidth="84" Click="OnCancel"/>
        <Button x:Name="SecondaryButton" Style="{StaticResource SW.Button.Danger}" Margin="10,0,0,0"
                MinWidth="110" Click="OnSecondary" Visibility="Collapsed"/>
        <Button x:Name="PrimaryButton" Style="{StaticResource SW.Button.Primary}" Margin="10,0,0,0"
                MinWidth="110" Click="OnPrimary"/>
      </StackPanel>
    </StackPanel>
  </Border>
</Window>
